# Scratch：可保留的探索工作区

这里保存 EXPLORE 阶段的脚本、推导辅助、可视化和小型输出。它在项目内，因此不会因系统
清理而丢失；除说明文件外，其内容不会提交到 Git。

每项探索放在：

```text
scratch/<task-id>/
```

探索结束后，结果会在 Closeout Plan 中决定：进入正式维护、保留用于复现，或在你确认后删除。
不要把这里当作正式代码、唯一数据副本或论文证据的永久位置。

详细边界见本目录的 `AGENTS.md`。
