# Protocols：冻结的验证方法

当某个 Claim 准备接受论文级检验时，在这里定义 Protocol。Protocol 说明使用什么数据、对照、
指标和判定规则来验证，但不保存实际运行结果。

它只服务 FORMAL：普通探索、临时试验和脚本输出不需要放进这里。冻结后若改变核心判定条件，
应视为新版本或新 Protocol，而不是覆盖过去的验证条件。

编号、状态和详细格式见本目录的 `AGENTS.md`。
