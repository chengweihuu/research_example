# Src：正式 Python 参考实现

这里放已经接受并愿意长期维护的 Python 代码，而不是随手验证想法的脚本。

当一个理论或工程能力已经经过 BUILD 验证后，代码才会进入本目录。将来会按实际需要形成
算法、外部适配、评估和工作流等模块；目前不预建空目录。

如果你只是想快速试一个计算或算法方向，应使用 `scratch/`。涉及 ROS 运行环境的正式部署
会在需要时进入独立的 `ros2_ws/`。

本目录的 `AGENTS.md` 规定正式代码的准入和职责边界。
