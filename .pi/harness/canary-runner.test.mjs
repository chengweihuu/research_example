import assert from "node:assert/strict";import test from "node:test";import{projectCanary}from"./canary-runner.mjs";
const m={id:"gpt-5.6-sol",provider:"openai-codex",api:"responses",maxTokens:4096,cost:{input:3,output:18}},base={runId:"R-test",provider:"openai-codex",modelId:m.id,catalogModel:m,serializedBytes:100,outputTokens:20,totalTokenCap:2900,usdCap:.05,stages:["before_request","agent_start","stream_start","assistant_message","stream_end","agent_end","after_request","after_settlement"],sseEvents:["opened","usage_complete","done"],assistant:{stopReason:"stop",usage:{input:10,output:2}}};
test("projects one bounded successful Canary",()=>{let n=0;const r=projectCanary({...base,invoke:()=>n++});assert.equal(n,1);assert.equal(r.requestCount,1);assert.equal(r.settlement.state,"provider_reported");assert.equal(r.ledgerVerification.valid,true);});
test("rejects mismatch before invoking",()=>{let n=0;assert.throws(()=>projectCanary({...base,modelId:"wrong",invoke:()=>n++}));assert.equal(n,0);});
